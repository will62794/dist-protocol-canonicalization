name: Compile LaTeX document

on:
  push:
    branches:
      - main  # Adjust this to your main branch name

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
      
      - name: Install LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install texlive-full -y  # Installs full TeXLive distribution
      
      - name: Compile LaTeX document
        run: |
          cd paper
          latexmk -pdf -f main.tex
          # Add additional commands if needed, e.g., bibtex, makeindex, etc.
      
      - name: Delete old releases
        uses: dev-drprasad/delete-older-releases@v0.3.4
        with:
          keep_latest: 0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: paper-${{ github.ref }}
          release_name: Release latest-main
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} 
          asset_path: ./paper/main.pdf
          asset_name: main.pdf
          asset_content_type: pdf

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v2
        with:
          name: output-pdf
          path: paper/main.pdf  # Replace with your main output PDF file
